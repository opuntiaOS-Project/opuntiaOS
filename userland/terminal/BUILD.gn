import("//build/userland/TEMPLATE.gni")

oneOS_executable("terminal") {
  install_path = "bin/"
  sources = [
    "TerminalView.cpp",
    "main.cpp",
  ]
  configs = [
    "//build/userland:userland_flags",
  ]
  deplibs = [
    "libcxx",
    "libfoundation",
    "libg",
    "libui"
  ]
}
