import("//build/libs/TEMPLATE.gni")

oneOS_static_library("libfoundation") {
  sources = [
    "EventLoop.cpp",
    "puff.c",
  ]

  deplibs = [ "libcxx" ]
  configs = [ "//build/libs:libcxx_flags" ]

  if (host == "llvm") {
    cflags = [ "-flto" ]
  }
}
